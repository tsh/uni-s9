$(function (){
    //Carousel
    $('.carousel').carousel({
        interval: 3200
    });

    //navbarSearch
    $( "#navbarSearch" ).submit(function( event ) {
//        alert( "Handler for .submit() called." );
//        event.preventDefault();
    });
})
